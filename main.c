#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "Libs/ordenacao.h"

int main(){
    Dados dados;
    int N, i;
    N = 15;
    srand(time(NULL));
    //cria_vetor(&dados, N);
    //preenche_vetor(&dados, N);
    //QuickSort_Recursivo(&dados, N);
    system("pause");
    return 0;
}